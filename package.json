{
  "name": "bluesky-thread-reader",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --watch src/server.ts",
    "build": "bun build src/server.ts --outdir dist"
  },
  "dependencies": {
    "@atproto/api": "^0.6.0",
    "@atproto/oauth-client-node": "^0.2.8",
    "@prisma/client": "^6.2.1",
    "elysia": "^1.2.10"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prisma": "^6.2.1"
  }
}
